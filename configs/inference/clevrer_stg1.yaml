# Global configuration
seed: 0
experiment_dir: "./experiments/clevrer_stg1"
run_name: version_0
num_images: 16
backend_engine:
  _target_: wiskers.backends.AECheckpointInference

# Data module (Test only)
data_module:
  _target_: wiskers.datasets.clevrer.ClevrerImage
  data_dir: "./datasets"
  batch_size: 4
  num_workers: 2
  image_size: [80, 120]
  preprocessing:
    _target_: wiskers.datasets.clevrer.PreprocessingConfig
    chunk_size: 16
    stride: 16
    resize: [160, 240]
    limit: null

# # Model and output paths
best_model_path: null
output_dir: null


