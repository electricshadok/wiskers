# Global seed
seed: 0

best_models_dir: "./experiments/clevrer_stg1"
run_name: version_0
module_type: "ae"
num_images: 16

best_model_path:
  _target_: wiskers.predict.PredictCLI.get_model_path
  best_models_dir: ${best_models_dir}
  run_name: ${run_name}
  model_format: "ckpt" # onnx/ckpt/safetensors

output_dir: 
  _target_: wiskers.predict.PredictCLI.get_output_dir
  best_models_dir: ${best_models_dir}
  run_name: ${run_name}

